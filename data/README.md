# Datos

Este directorio queda reservado para datasets públicos y reproducibles.

No se deben subir API keys, datos privados ni credenciales.

La aplicación debe preferir datos históricos con fecha de corte explícita y ejecutar backtesting walk-forward para evitar leakage.
